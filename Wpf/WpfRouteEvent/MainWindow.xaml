<Window x:Class="WpfRouteEvent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfRouteEvent"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="200">
    <Grid x:Name="gridRoot" Background="Linen">
        <Grid x:Name="gridA" Margin="10" Background="Blue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <Canvas Grid.Column="0" Margin="10" Background="Pink">
                <Button x:Name="buttonLeft" Content="Left" Margin="10" Background="Gray" Width="40" Height="100" />
            </Canvas>
            <Canvas x:Name="canvasB" Grid.Column="1" Margin="10" Background="Pink">
                <local:TimeButton x:Name="buttonRight" Content="Right" Margin="10" Background="Gray" Width="40" Height="100" />
            </Canvas>
        </Grid>
    </Grid>
</Window>
